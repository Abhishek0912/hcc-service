spring:
#  datasource:
#    url: jdbc:mysql://hcc-dev-rds.ch2uy8ukk87u.us-east-1.rds.amazonaws.com:3306/hccdb
#    username: hccAdmin
#    password: hccAdmin123
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_e88cSmHdd/.well-known/jwks.json
          issuer-uri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_e88cSmHdd

db:
  secret:
    name: hcc/dev/db

aws:
  region: us-east-1
